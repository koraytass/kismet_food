---
/**
 * SiteFooter — v2 (Trade Floor).
 * 4 kolon kurumsal footer kaldırıldı; tek-satır hairline footer.
 * Mevcut tüm sayfalar bu dosya üzerinden TradeFloorFooter alır.
 */
import type { Locale } from '@/i18n';
import TradeFloorFooter from './v2/TradeFloorFooter.astro';

interface Props { locale: Locale }
const { locale } = Astro.props;
---

<TradeFloorFooter locale={locale} />
