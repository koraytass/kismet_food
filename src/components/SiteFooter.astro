---
/**
 * SiteFooter — v3 (Corporate / LDC).
 * Annual-report grade çok kolonlu footer; CorpFooter delegated.
 */
import type { Locale } from '@/i18n';
import CorpFooter from './v3/CorpFooter.astro';

interface Props { locale: Locale }
const { locale } = Astro.props;
---

<CorpFooter locale={locale} />
