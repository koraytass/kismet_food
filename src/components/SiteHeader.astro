---
/**
 * SiteHeader — v3 (Corporate / LDC).
 * Tüm sayfalar için tek nav + mobil overlay; CorpNav delegated.
 */
import type { Locale } from '@/i18n';
import CorpNav from './v3/CorpNav.astro';

interface Props {
  locale: Locale;
  currentPath: string;
}

const { locale, currentPath } = Astro.props;
---

<CorpNav locale={locale} currentPath={currentPath} />
