---
import BaseLayout from '@/layouts/BaseLayout.astro';
import SEOHead from '@/components/SEOHead.astro';
import TradeFloorHome from '@/components/v2/TradeFloorHome.astro';
import { t } from '@/i18n';
import { organizationSchema, webSiteSchema } from '@/lib/schema';

const locale = 'ar';
const translations = t(locale);
const jsonLd = [organizationSchema(locale), webSiteSchema()];
---

<BaseLayout locale={locale}>
  <Fragment slot="head">
    <SEOHead
      title={translations.meta.title}
      description={translations.meta.description}
      locale={locale}
      pathname="/ar/"
      ogTitle={translations.meta.ogTitle}
      ogDescription={translations.meta.ogDescription}
      jsonLd={jsonLd}
    />
  </Fragment>

  <TradeFloorHome locale={locale} currentPath={Astro.url.pathname} />
</BaseLayout>
